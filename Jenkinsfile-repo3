pipeline {

    agent any

    stages {

        stage('clone repo3') {

            steps {
                git branch: '2026Q1.1',
                url: 'https://github.com/007uikey/repo3.git'

            }
        }

        stage('Delete old websites') {

            steps {

                sh 'rm -rf /var/www/html/*'

            }
        }

        stage('Deploy repo1 and set permission') {

            steps {

                sh 'sudo cp -r * /var/www/html/'
                sh 'sudo chmod -R 777 /var/www/html'
            }
        }
    }
}
