pipeline {
    agent any

    stages {

        stage('Clean Old Deployment') {
            steps {
                sh 'rm -rf /var/www/html/*'
            }
        }

        stage('Deploy New Files') {
            steps {
                sh 'cp -r * /var/www/html/'
                sh 'sudo chown -R apache:apache /var/www/html'
            }
        }

    }

    post {
        success {
            echo 'Repo3 deployed successfully'
        }
    }
}
